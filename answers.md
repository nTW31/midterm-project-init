# คำตอบข้อสอบกลางภาค ส่วนที่ 2 (ข้อที่ 5: คำถามวิเคราะห์)

## 1. ทำไมการทำ Sorting ใน SQL จึงต้องใช้ Allowlist แทน Parameterized Query?

Parameterized Query (การใช้ `?` หรือ Prepared Statements) ถูกสร้างขึ้นเพื่อจัดการกับ **"ค่าข้อมูล (Data Literals/Values)"** ในคำสั่ง SQL เช่น เงื่อนไขใน `WHERE` หรือค่าใน `INSERT INTO ... VALUES (?)` โดยตัว Database Engine จะทำการคอมไพล์โครงสร้างคำสั่งไว้ล่วงหน้า ทำให้ค่าข้อมูลไม่สามารถแทรกแซงไวยากรณ์ SQL ได้

อย่างไรก็ตาม **ชื่อคอลัมน์ (Identifier)** และคีย์เวิร์ดการเรียงลำดับ (`ASC` / `DESC`) ในคำสั่ง `ORDER BY` จัดเป็น **"โครงสร้างไวยากรณ์ของคำสั่ง SQL"** ซึ่งไวยากรณ์ฐานข้อมูลไม่อนุญาตให้ใช้พารามิเตอร์ `?` แทนที่ได้ หากนำค่าจาก `req.query.sort` ไปต่อสตริงโดยตรง (String Concatenation) จะเปิดช่องโหว่ SQL Injection ทันที

ดังนั้น การใช้ **Allowlist Validation** (ตรวจสอบกับรายการที่กำหนดไว้ล่วงหน้า เช่น `ALLOWED_SORT_FIELDS`) จึงเป็นวิธีเดียวที่ปลอดภัยและมีประสิทธิภาพในการรับประกันว่าค่าที่นำไปประกอบในคำสั่ง SQL เป็นชื่อคอลัมน์ที่ระบบอนุญาตจริง ๆ

---

## 2. ความสำคัญของ Database Transaction ในการเขียนหลายตารางที่สัมพันธ์กัน

ในการเพิ่มวิชาใหม่ จำเป็นต้องเขียนข้อมูลลง 2 ตารางที่สัมพันธ์กัน ได้แก่ `courses` (ข้อมูลรายวิชา) และ `course_prerequisites` (วิชาบังคับก่อน)

หากไม่ใช้ Transaction และเกิดข้อผิดพลาดขึ้นในระหว่างการบันทึก `course_prerequisites` (เช่น รหัสวิชาบังคับก่อนไม่ถูกต้อง หรือเครือข่ายขัดข้อง) ข้อมูลในตาราง `courses` จะถูกบันทึกค้างไว้แล้ว ทำให้เกิดข้อมูลกำพร้า (Orphan Record) และฐานข้อมูลสูญเสียความสอดคล้องตามหลัก **Consistency**

การใช้ Database Transaction ผ่าน `pool.getConnection()`, `beginTransaction()`, `commit()`, และ `rollback()` ช่วยรับประกันคุณสมบัติ **Atomicity** ตามหลัก ACID คือ "ต้องสำเร็จทั้งหมด หรือไม่สำเร็จเลย (All or Nothing)" หากเกิดข้อผิดพลาดใด ๆ ระบบจะยกเลิกการกระทำทั้งหมดกลับสู่สถานะเดิม 100% และการใช้บล็อก `finally { conn.release(); }` จะช่วยคืน connection กลับสู่ Connection Pool เพื่อป้องกันปัญหา Connection Leak

---

## 3. ปัญหาของ In-memory Cache เมื่อนำไปรันในสภาพแวดล้อมแบบ Multi-instance และประโยชน์ของ Redis

In-memory Cache (เช่น การเก็บตัวแปรใน RAM ของ Node.js process) ผูกติดอยู่กับหน่วยความจำของ Process แต่ละตัวแยกกันอย่างอิสระ เมื่อระบบขยายตัวในแนวราบ (Horizontal Scaling) โดยมีหลาย Instance ทำงานอยู่หลัง Load Balancer จะเกิดปัญหาสำคัญ 2 ประการ:

1. **Cache Inconsistency (ข้อมูลไม่สอดคล้องกัน):** เมื่อ Instance A ได้รับคำสั่งอัปเดตข้อมูลและสั่งลบแคชในเครื่องตนเอง แต่ Instance B ยังคงถือแคชเดิมอยู่ ผู้ใช้งานที่ยิงคำขอถัดมาแล้วถูกส่งไป Instance B จะได้รับข้อมูลที่ล้าสมัย (Stale Data)
2. **สิ้นเปลืองทรัพยากร:** ทุก Instance ต้องเก็บข้อมูลแคชชุดเดียวกันซ้ำซ้อนกันใน RAM ของตัวเอง

การเปลี่ยนมาใช้ **Redis** ซึ่งเป็น **Centralized Distributed Cache** ทำให้ทุก Instance แชร์แคชแหล่งเดียวกันจากศูนย์กลาง เมื่อ Instance ใดสั่ง Invalidate หรืออัปเดตแคช ทุก Instance จะมองเห็นการเปลี่ยนแปลงล่าสุดตรงกันทันที

---

## 4. แนวทางการจัดการ API Deprecation เมื่อเปิดตัว v2 โดยไม่กระทบผู้ใช้งาน v1

1. **กำหนดนโยบายและการประกาศล่วงหน้า (Deprecation Policy & Grace Period):** แจ้งเตือนผู้พัฒนาล่วงหน้าอย่างเป็นทางการ พร้อมระบุระยะเวลาเปลี่ยนผ่านที่ชัดเจน (เช่น 6–12 เดือน) ว่า v1 จะยุติการให้บริการในวันใด
2. **ส่ง HTTP Headers แจ้งเตือนใน Response ของ v1:**
   - `Deprecation: true` หรือ `@<timestamp>` เพื่อแจ้งเตือนว่า endpoint นี้ล้าสมัยแล้ว
   - `Sunset: <date>` (ตามมาตรฐาน RFC 8594) เพื่อระบุวันที่แน่นอนที่จะปิดให้บริการถาวร
   - `Link: </api/v2/courses>; rel="successor-version"` เพื่อชี้เป้าไปยัง API เวอร์ชันใหม่
3. **การทำงานแบบคู่ขนาน (Dual-running):** รันทั้ง `/api/v1` และ `/api/v2` ควบคู่กันไปตลอดช่วงระยะเวลาเปลี่ยนผ่าน เพื่อให้ระบบเดิมทำงานได้อย่างราบรื่น
4. **การติดตามการใช้งาน (Telemetry & Traffic Monitoring):** มอนิเตอร์ Log ว่ายังมีลูกค้ารายใดที่ยังเรียกใช้ v1 อยู่ และส่งอีเมลหรือการแจ้งเตือนโดยตรงก่อนถึงวันปิดระบบจริง
